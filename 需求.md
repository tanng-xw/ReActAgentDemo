# 需求概述
实现一个智能助手，提供自然语言的对话式交互。支持多轮思考和工具调用，实现复杂任务。

# 前端要求
1. 支持多轮对话
2. 助手每次返回给用户的内容分为两个部分：
   1. 思考过程：模型的中间思考结果和工具调用。
   2. 回答：给用户的最终回答。
3. 页面不刷新，则始终保持在同一会话上下文中。页面刷新则启动新会话上下文。

# 后端要求
1. 基于Java 21和Spring AI实现。
2. 对用户的每次输入，支持多轮思考和调用工具，最终给用户回答。
3. 同一会话中保持上下文记忆。
4. Agent与模型的交互必须都通过Spring AI接口完成，比如工具调用应该使用模型原生的function call。

# 交互流程
1. 用户输入 → 助手思考（模型返回纯文本，显示在思考过程UI）
2. 助手思考 → 工具调用（模型返回tool_calls，后端执行工具后结果将结果以标准格式返回给模型。前端思考过程中显示一句“为您调用XXX工具。”）
3. 工具结果 → 继续思考或最终回答（模型根据结果决定继续思考、调用工具、或者输出最终回答）

需要注意的是，模型思考的文本和最终回答的文本都是通过OpenAI标准接口格式的"content"字段返回的，因此需要加以区分：思考文本模型正常输出，最终回答文本模型以`回答：xxxxx`格式输出。

# 支持工具
1. 支持多种类型的工具，可以在代码中简单扩展
2. 支持对工具描述、参数描述的修改，可以在代码中完成。
3. 预置部分工具：
   1. 用户地点查询工具，参数为会话ID，返回值根据会话ID哈希在北京、上海、杭州中选一个。
   2. 天气查询工具，参数为地点，返回值根据地点预置一些数据，支持地点北京、上海、杭州。
   3. 歌曲搜索工具，参数为关键词，返回值是歌曲列表，歌曲数据结构为：歌曲ID、歌名、歌手名、专辑名。预置一些数据。

# 其他要求
1. 提供完整的启动、停止、重启脚本，Windows上运行的。
2. 代码中的注释应该使用中文

# 验收标准
1. 遵循TDD原则，所有功能均要有完善的测试用例，并要求全部测试通过。

---

# Claude Code/Kimi Code要求
以下是给Claude Code/Kimi Code的要求：
1. 每次响应后面增加一句“主人，请继续命令我”
2. 使用中文与我对话